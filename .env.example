# Reason reads these variables only. Sense and Act never use an LLM.
LLM_PROVIDER=ollama
LLM_MODEL=qwen:14b
OLLAMA_BASE_URL=http://localhost:11434

# Only needed when LLM_PROVIDER=openai
OPENAI_API_KEY=
